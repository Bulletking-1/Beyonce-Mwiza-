<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Mwiya Beyonce Mwiza - My Sincere Apology</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        body {
            background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
            color: #f1f1f1;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 20px;
            overflow-x: hidden;
        }
        
        .container {
            max-width: 900px;
            width: 100%;
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.5);
            border: 1px solid rgba(255, 255, 255, 0.1);
            position: relative;
            overflow: hidden;
        }
        
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 2px solid rgba(255, 215, 0, 0.3);
            position: relative;
        }
        
        .title {
            font-size: 2.8rem;
            background: linear-gradient(to right, #FFD700, #FFA500, #FF6347);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin-bottom: 10px;
            text-shadow: 0 5px 15px rgba(255, 215, 0, 0.2);
            letter-spacing: 1px;
        }
        
        .name-highlight {
            font-size: 3.2rem;
            background: linear-gradient(45deg, #ff9a9e, #fad0c4, #fad0c4, #a1c4fd);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            display: block;
            margin: 15px 0;
            font-weight: 700;
            text-shadow: 0 0 20px rgba(255, 154, 158, 0.3);
            animation: glow 3s ease-in-out infinite alternate;
        }
        
        @keyframes glow {
            0% { text-shadow: 0 0 10px rgba(255, 154, 158, 0.3); }
            100% { text-shadow: 0 0 25px rgba(255, 154, 158, 0.6), 0 0 35px rgba(161, 196, 253, 0.4); }
        }
        
        .heart-container {
            display: flex;
            justify-content: center;
            margin: 20px 0;
            gap: 15px;
            flex-wrap: wrap;
        }
        
        .heart {
            color: #ff6b8b;
            font-size: 2.5rem;
            animation: float 4s ease-in-out infinite;
            text-shadow: 0 0 15px rgba(255, 107, 139, 0.5);
        }
        
        .heart:nth-child(2) { animation-delay: 0.5s; color: #ff9a00; }
        .heart:nth-child(3) { animation-delay: 1s; color: #00d4ff; }
        .heart:nth-child(4) { animation-delay: 1.5s; color: #9d4edd; }
        .heart:nth-child(5) { animation-delay: 2s; color: #ffd166; }
        
        @keyframes float {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(10deg); }
        }
        
        .apology-content {
            background: rgba(0, 0, 0, 0.2);
            border-radius: 15px;
            padding: 25px;
            margin: 25px 0;
            border-left: 5px solid #ff6b8b;
            box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.3);
        }
        
        .apology-text {
            font-size: 1.2rem;
            line-height: 1.7;
            text-align: center;
            margin-bottom: 20px;
        }
        
        .apology-text p {
            margin-bottom: 15px;
        }
        
        .promise-box {
            background: linear-gradient(135deg, rgba(255, 107, 139, 0.15), rgba(157, 78, 221, 0.15));
            border: 2px dashed #9d4edd;
            border-radius: 15px;
            padding: 20px;
            text-align: center;
            margin: 25px 0;
        }
        
        .promise-title {
            font-size: 1.8rem;
            color: #ffd166;
            margin-bottom: 15px;
        }
        
        .promise-text {
            font-size: 1.4rem;
            font-weight: 600;
            color: #00d4ff;
            text-shadow: 0 0 10px rgba(0, 212, 255, 0.3);
        }
        
        .forgiveness-game {
            background: rgba(255, 255, 255, 0.08);
            border-radius: 15px;
            padding: 25px;
            margin: 30px 0;
            text-align: center;
        }
        
        .game-title {
            font-size: 1.8rem;
            color: #ff9a00;
            margin-bottom: 20px;
        }
        
        .game-instruction {
            font-size: 1.1rem;
            margin-bottom: 25px;
            color: #ccc;
        }
        
        .heart-grid {
            display: grid;
            grid-template-columns: repeat(5, 1fr);
            gap: 15px;
            margin-bottom: 25px;
        }
        
        .game-heart {
            width: 60px;
            height: 60px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            cursor: pointer;
            transition: all 0.3s ease;
            user-select: none;
        }
        
        .game-heart:hover {
            background: rgba(255, 107, 139, 0.3);
            transform: scale(1.1);
        }
        
        .game-heart.clicked {
            background: rgba(255, 107, 139, 0.5);
            color: #ff6b8b;
            transform: scale(1.1);
            box-shadow: 0 0 15px rgba(255, 107, 139, 0.7);
        }
        
        .game-message {
            font-size: 1.3rem;
            color: #ffd166;
            min-height: 30px;
            margin-top: 15px;
            font-weight: 600;
            transition: all 0.5s ease;
        }
        
        .forgiveness-btn {
            background: linear-gradient(to right, #ff6b8b, #ff9a00);
            border: none;
            color: white;
            padding: 15px 40px;
            font-size: 1.3rem;
            border-radius: 50px;
            cursor: pointer;
            margin: 20px auto;
            display: block;
            font-weight: 600;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            box-shadow: 0 5px 15px rgba(255, 107, 139, 0.4);
        }
        
        .forgiveness-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(255, 107, 139, 0.6);
            background: linear-gradient(to right, #ff9a00, #ff6b8b);
        }
        
        .forgiveness-btn:active {
            transform: translateY(0);
        }
        
        .signature {
            text-align: right;
            margin-top: 30px;
            padding-top: 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            font-style: italic;
            color: #aaa;
        }
        
        .footer {
            text-align: center;
            margin-top: 30px;
            color: #888;
            font-size: 0.9rem;
        }
        
        .confetti {
            position: absolute;
            width: 15px;
            height: 15px;
            opacity: 0;
        }
        
        @media (max-width: 768px) {
            .title { font-size: 2.2rem; }
            .name-highlight { font-size: 2.5rem; }
            .heart-grid { grid-template-columns: repeat(3, 1fr); }
            .game-heart { width: 70px; height: 70px; }
            .container { padding: 20px; }
        }
        
        @media (max-width: 480px) {
            .title { font-size: 1.8rem; }
            .name-highlight { font-size: 2rem; }
            .heart-grid { grid-template-columns: repeat(2, 1fr); }
            .game-heart { width: 80px; height: 80px; }
            .apology-text { font-size: 1.1rem; }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title">A Sincere Apology</h1>
            <div class="heart-container">
                <div class="heart"><i class="fas fa-heart"></i></div>
                <div class="heart"><i class="fas fa-heart"></i></div>
                <div class="heart"><i class="fas fa-heart"></i></div>
                <div class="heart"><i class="fas fa-heart"></i></div>
                <div class="heart"><i class="fas fa-heart"></i></div>
            </div>
            <span class="name-highlight">Mwiya Beyonce Mwiza</span>
            <p>This message is from my heart to yours</p>
        </div>
        
        <div class="apology-content">
            <div class="apology-text">
                <p>Dear <strong>Mwiya Beyonce Mwiza</strong>,</p>
                
                <p>I am writing this with a heart full of regret and sincere remorse for my words that hurt you. I deeply apologize for what I said, and I want you to know that I understand how my words affected you.</p>
                
                <p>You are an incredible person with a beautiful heart and amazing character. You don't deserve to be treated with anything less than the utmost respect, kindness, and love.</p>
                
                <p>I realize now how much my words contradicted my true feelings for you, and I am truly ashamed of having said them.</p>
            </div>
            
            <div class="promise-box">
                <div class="promise-title">My Promise to You</div>
                <div class="promise-text">
                    "I will never say bad words to you again."
                </div>
            </div>
            
            <div class="apology-text">
                <p>This isn't just an empty promise. I am committed to changing how I communicate, especially when I'm upset or frustrated. You mean too much to me to let my temper or thoughtlessness damage what we have.</p>
                
                <p>I am asking for your forgiveness, not because I deserve it, but because I truly want to make things right and be a better person for you.</p>
            </div>
        </div>
        
        <div class="forgiveness-game">
            <h2 class="game-title">A Game for Your Heart</h2>
            <p class="game-instruction">Click on the hearts to reveal my message and show that you accept my apology</p>
            
            <div class="heart-grid" id="heartGrid">
                <!-- Hearts will be generated by JavaScript -->
            </div>
            
            <div class="game-message" id="gameMessage"></div>
            
            <button class="forgiveness-btn" id="forgivenessBtn">
                <i class="fas fa-heart"></i> I Ask For Your Forgiveness <i class="fas fa-heart"></i>
            </button>
        </div>
        
        <div class="signature">
            <p>With sincere regret and hope,</p>
            <p style="color: #ff9a00; font-size: 1.2rem; margin-top: 5px;">From the one who cares about you</p>
        </div>
        
        <div class="footer">
            <p>This apology is digital, but my regret and commitment are real.</p>
            <p><i class="far fa-copyright"></i> Created with sincerity and hope for reconciliation</p>
        </div>
    </div>

    <script>
        // Create the heart grid for the game
        const heartGrid = document.getElementById('heartGrid');
        const gameMessage = document.getElementById('gameMessage');
        const forgivenessBtn = document.getElementById('forgivenessBtn');
        
        // Messages that appear when hearts are clicked
        const heartMessages = [
            "I'm Sorry", "Forgive Me", "I Was Wrong", "You Deserve Better", 
            "I Regret It", "My Bad Words", "Will Never", "Be Repeated", 
            "Promise", "To You", "Mwiya", "Beyonce", "Mwiza", 
            "Respect", "Always", "From Now", "Onward"
        ];
        
        // Initialize the heart grid
        for (let i = 0; i < 15; i++) {
            const heartDiv = document.createElement('div');
            heartDiv.className = 'game-heart';
            heartDiv.innerHTML = '<i class="fas fa-heart"></i>';
            heartDiv.dataset.message = heartMessages[i] || "I'm Sorry";
            
            heartDiv.addEventListener('click', function() {
                if (!this.classList.contains('clicked')) {
                    this.classList.add('clicked');
                    this.innerHTML = this.dataset.message;
                    
                    // Check if all hearts are clicked
                    const allHearts = document.querySelectorAll('.game-heart');
                    const clickedHearts = document.querySelectorAll('.game-heart.clicked');
                    
                    if (clickedHearts.length === allHearts.length) {
                        gameMessage.textContent = "You've unlocked my complete apology. Thank you for hearing me out.";
                        gameMessage.style.color = "#00ff88";
                        gameMessage.style.fontSize = "1.5rem";
                        
                        // Create confetti effect
                        createConfetti();
                    } else {
                        // Show encouraging message based on progress
                        const progress = clickedHearts.length / allHearts.length;
                        if (progress > 0.7) {
                            gameMessage.textContent = "Almost there! Thank you for your patience.";
                        } else if (progress > 0.4) {
                            gameMessage.textContent = "Thank you for listening to my apology.";
                        } else {
                            gameMessage.textContent = "Thank you for clicking. Each heart holds part of my promise.";
                        }
                    }
                }
            });
            
            heartGrid.appendChild(heartDiv);
        }
        
        // Forgiveness button click event
        forgivenessBtn.addEventListener('click', function() {
            // Change button text and style
            this.innerHTML = '<i class="fas fa-heart"></i> Thank You For Your Forgiveness <i class="fas fa-heart"></i>';
            this.style.background = 'linear-gradient(to right, #00ff88, #00d4ff)';
            this.style.boxShadow = '0 5px 15px rgba(0, 255, 136, 0.4)';
            
            // Show final message
            gameMessage.textContent = "Mwiya Beyonce Mwiza, thank you from the bottom of my heart. I will honor my promise always.";
            gameMessage.style.color = "#00ff88";
            gameMessage.style.fontSize = "1.6rem";
            gameMessage.style.fontWeight = "700";
            
            // Create confetti celebration
            createConfetti();
            
            // Mark all hearts as clicked
            const allHearts = document.querySelectorAll('.game-heart');
            allHearts.forEach(heart => {
                if (!heart.classList.contains('clicked')) {
                    heart.classList.add('clicked');
                    heart.innerHTML = heart.dataset.message;
                }
            });
            
            // Disable button after click
            this.disabled = true;
        });
        
        // Confetti effect function
        function createConfetti() {
            const colors = ['#ff6b8b', '#ff9a00', '#00d4ff', '#9d4edd', '#ffd166', '#00ff88'];
            
            for (let i = 0; i < 100; i++) {
                const confetti = document.createElement('div');
                confetti.className = 'confetti';
                confetti.style.left = Math.random() * 100 + 'vw';
                confetti.style.top = '-20px';
                confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
                confetti.style.width = Math.random() * 15 + 5 + 'px';
                confetti.style.height = confetti.style.width;
                confetti.style.borderRadius = Math.random() > 0.5 ? '50%' : '0';
                document.body.appendChild(confetti);
                
                // Animate confetti
                const animation = confetti.animate([
                    { transform: 'translateY(0) rotate(0deg)', opacity: 1 },
                    { transform: `translateY(${window.innerHeight + 100}px) rotate(${Math.random() * 720}deg)`, opacity: 0 }
                ], {
                    duration: Math.random() * 3000 + 2000,
                    delay: Math.random() * 1000
                });
                
                // Remove confetti after animation
                animation.onfinish = () => confetti.remove();
            }
        }
        
        // Add initial animation to name
        document.addEventListener('DOMContentLoaded', function() {
            const nameHighlight = document.querySelector('.name-highlight');
            nameHighlight.style.transform = 'scale(1.05)';
            setTimeout(() => {
                nameHighlight.style.transform = 'scale(1)';
            }, 300);
        });
    </script>
</body>
</html>
